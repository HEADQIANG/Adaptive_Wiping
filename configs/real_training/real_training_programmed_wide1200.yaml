schema_version: 1
profile: airbot_native_tared_offline
raw_data: runs/real_training/programmed_hold_last_tared_004/raw.h5
encoder: runs/sim_training/pretrain_wide_1200_v1/encoder.pt
output_dir: runs/real_training/training_programmed_wide1200_v1
wrench:
  frame: sensor local
  reference_point: sensor origin
  compensation: recorded_unloaded_baseline_subtracted
processing:
  max_age_s: 0.02
  max_gap_s: 0.02
  filter_order: 2
  filter_cutoff_hz: 1.0
training:
  device: cpu
  threads: 1
  seed: 42
  learning_rate: 0.001
  xy_batch_size: 8
  ft_batch_size: 32
  xy_epochs: 10000
  ft_epochs: 2000
  checkpoint_every: 100
