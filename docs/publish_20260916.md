# 2026-09-16 操作说明与发布验证

根目录 [README](../README.md) 已按仿真采集、仿真训练、真机探索、真机示教、
真机训练和真机部署六阶段更新，说明解释器、输入输出路径、交互按键、预检和续训方式。
本次发布包含此前的 runs 七类路径整理、联合 VAE 重训、人工采集与部署工作流代码。
`runs/`、`archive/`、`.runs_cleanup/`、采集数据及模型权重不随 Git 上传。

## 可重复安装与命令修正

补齐 robosuite 子模块上游地址，并在主仓库保存本地 AIRBOT 适配补丁及逐文件哈希。
在固定上游版本的临时 checkout 上验证了 23 个适配文件的恢复和重复执行，
已有本地适配的校验也通过。克隆/恢复步骤见
[robosuite 操作说明](sim_pretrain/robosuite_location.md)。

现有根目录启动脚本已移除；入口说明和回归测试统一为
`python -m scripts.<模块>`，见 [Python 入口](python_entrypoints.md)。
模型资源完整性测试按来源清单的既定范围检查运行时模型、惯量参考和许可文件；
`asserts/connector/` 的独立 CAD 文件保留，不改动模型或原来源哈希。

修复联合重训对 `dataset.h5` 符号链接的哈希查找：清单和查询均使用解析后的实际路径。
回归覆盖正常链接加载和数据被修改后拒绝训练；用现有 1200 条源数据进行了启动只读验证，
960/120/120 划分、检查点绑定及训练集归一化通过。本次发布验证未重新训练完整模型，
未连接真机或执行真机运动。操作命令保持不变，见
[联合重训说明](sim_pretrain/joint_vae_retraining.md)。

## 验证结果

- README 的 19 个 Bash 代码块语法检查和本地文档链接检查通过；13 条相关命令的帮助入口通过。
- 本次相关 17 项回归全部通过：模块入口、适配补丁、联合重训、模型资源。
- 集中模型资源 55 个文件的完整性校验通过。
- 全量回归：753 项，746 项通过、5 项跳过、1 项失败、1 项错误，耗时 167.487 秒。

当前环境未安装 Ruff，本轮未执行 Ruff 静态检查。

全量回归未全部通过，剩余项均在
`tests.sim_pretrain.test_comparison_collection.ComparisonCollectionTests`：

| 测试 | 结果 |
| --- | --- |
| `test_reused_environment_matches_fresh_second_assignment` | 触发 `100 mm / 20 degrees` 实验跟踪保护 |
| `test_vectorized_checks_match_saved_pilot_dynamics` | 与归档 pilot 的力数据不一致，最大绝对差约 3.30506 |

这两项与 [runs 整理时的复核结果](runs_layout.md#2026-09-16-整理结果与验证) 一致，
本次没有更改仿真物理模型、放宽断言或跳过这两项。
5 项跳过按测试环境条件执行；历史数据相关测试需本机归档，不是新克隆的无数据验收。

在项目根目录复核：

```bash
env -u PYTHONPATH OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  MUJOCO_GL=egl MPLCONFIGDIR=/tmp/wiping-publish-mpl \
  /home/wp/miniconda3/envs/clean/bin/python -m unittest discover -s tests -t . -q
python3 -m scripts.shared.robosuite_patch verify
python3 -m scripts.shared.assets verify
```

完整测试在当前环境预期因上述两项返回非零状态，不能把本次文档发布视作真机安全验收。
